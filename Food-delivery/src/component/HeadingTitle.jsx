import React from 'react'

const HeadingTitle = ({headingTitle}) => {
  return (
    <div>
      <h2 className='text-dark text-center py-6 text-xl font-semibold font-serif'>{headingTitle}</h2>
    </div>
  )
}

export default HeadingTitle
